# ============================================
# DOCKER COMPOSE ENVIRONMENT VARIABLES
# ============================================
# Configuration for Phase 3 Docker deployment
# Copy this file to .env and modify as needed

# ============================================
# DOCKER IMAGE CONFIGURATION
# ============================================
# Replace 'your-dockerhub-username' with your actual Docker Hub username
# Example: DOCKER_IMAGE=johndoe/product-app:latest
DOCKER_IMAGE=your-dockerhub-username/product-app:latest

# ============================================
# MONGODB CONFIGURATION
# ============================================
# MongoDB root credentials (used by both MongoDB container and web app)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your-secure-password-here
MONGO_DATABASE=productdb

# ============================================
# NOTES
# ============================================
# 1. DO NOT use MongoDB Atlas URI here
# 2. MongoDB runs as a Docker container in the same network
# 3. Connection string is automatically built in docker-compose.yml
# 4. Format: mongodb://username:password@mongodb:27017/database?authSource=admin
# 5. "mongodb" is the service name defined in docker-compose.yml
