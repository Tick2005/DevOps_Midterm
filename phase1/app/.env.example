# Application Configuration Example
# Copy this file to .env and modify as needed

# Server Port
PORT=3000

# MongoDB Atlas Configuration (Cloud Database) - REQUIRED
# Get your connection string from: https://www.mongodb.com/cloud/atlas
# Steps:
#   1. Sign up at https://www.mongodb.com/cloud/atlas (Free tier M0 available)
#   2. Create a cluster (Choose M0 Free - 512MB)
#   3. Create Database User (Database Access > Add New User)
#   4. Whitelist IP (Network Access > Add IP > 0.0.0.0/0)
#   5. Get connection string (Connect > Connect your application)
#
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
# Example: mongodb+srv://myuser:MyP@ssw0rd@cluster0.mongodb.net/productdb
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/productdb

# Node Environment
NODE_ENV=development
