# Application Configuration Example
# Copy this file to .env and modify as needed

# Server Port
PORT=3000

# MongoDB Atlas Configuration (Cloud Database) - REQUIRED
# The automation script (start.sh) will ask for your credentials:
#   - MongoDB Username
#   - MongoDB Password  
#   - Cluster Name
#   - Database Name (default: productdb)
#
# How to get credentials:
#   1. Sign up at https://www.mongodb.com/cloud/atlas (Free M0 tier)
#   2. Create a cluster
#   3. Create Database User (Database Access)
#   4. Whitelist IP: 0.0.0.0/0 (Network Access)
#   5. Note your cluster name from Clusters page
#
# The script will automatically build the connection string:
# mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>

# Node Environment
NODE_ENV=development
